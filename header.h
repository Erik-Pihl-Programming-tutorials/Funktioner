/********************************************************************************
* header.h: Innehåller funktionalitet för beräkning samt utskrift av flyttal.
********************************************************************************/
#ifndef HEADER_H_
#define HEADER_H_

/* Inkluderingsdirektiv: */
#include <stdio.h>

/* Externa funktioner: */
double get_sum(const double x,
               const double y,
               const double z);
void print_numbers(const double x,
                   const double y,
                   const double z);

#endif /* HEADER_H_ */